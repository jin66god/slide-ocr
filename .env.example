# 复制为 .env 后 docker compose 会读取
PORT=8118
WORKERS=2
SIMPLE_TARGET=1
LOG_LEVEL=INFO
HTTP_TIMEOUT=20
MEM_LIMIT=8g
CPUS=3.5

# ===== 鉴权（必填，防止公网滥用）=====
# 自己设一个长随机串，例如: openssl rand -hex 24
API_KEY=change-me-to-a-long-random-secret

# 1=强制鉴权（默认）；0=允许无密钥匿名（仅本地调试）
REQUIRE_API_KEY=1
